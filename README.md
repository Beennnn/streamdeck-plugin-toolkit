# streamdeck-plugin-toolkit — merged into `streamdeck-toolkit`

> **This repo has moved.** As of 2026-07-17 the plugin toolkit was merged with
> the icon-pack toolkit into a single repo so they can share one core:
>
> ### → **https://github.com/Beennnn/streamdeck-toolkit**
>
> The plugin tool now lives there as `sdplugin` (`bin/sdplugin verify|fix`), the
> icon tool as `sdicons`, over a shared `sdcommon` core. The plugin build
> reference (this repo's old README) is at
> [`docs/PLUGINS.md`](https://github.com/Beennnn/streamdeck-toolkit/blob/main/docs/PLUGINS.md);
> the Marketplace review playbook at
> [`docs/MARKETPLACE-REVIEW.md`](https://github.com/Beennnn/streamdeck-toolkit/blob/main/docs/MARKETPLACE-REVIEW.md).
>
> This repository is archived (read-only). Its history is preserved here and was
> also merged into `streamdeck-toolkit`.
